# Firebase 푸시 성능 테스트 지표 보고서

이 문서는 Firebase Cloud Functions를 통한 푸시 알림 전송 시, 순차(Sequential) 처리와 병렬(Parallel) 처리의 성능 차이를 비교한 테스트 결과 요약입니다.

## 📊 테스트 결과 요약 (수신자 수: 100명)

| 지표                      | 순차 처리 (평균) | 병렬 처리 (평균) | 개선 정도          |
| :------------------------ | :--------------- | :--------------- | :----------------- |
| **전체 소요 시간 (ms)**   | 1,129.83 ms      | 426.93 ms        | **약 2.65배 빠름** |
| **토큰당 평균 시간 (ms)** | 11.30 ms         | 4.27 ms          | **약 2.65배 빠름** |

---

## 🔍 케이스별 상세 데이터

### CASE 1

- **순차(Sequential)**: `total_ms: 1282.96`, `avg_ms_per_token: 12.83`
- **병렬(Parallel)**: `total_ms: 467.63`, `avg_ms_per_token: 4.68`

### CASE 2

- **순차(Sequential)**: `total_ms: 1054.90`, `avg_ms_per_token: 10.55`
- **병렬(Parallel)**: `total_ms: 395.70`, `avg_ms_per_token: 3.96`

### CASE 3

- **순차(Sequential)**: `total_ms: 1051.64`, `avg_ms_per_token: 10.52`
- **병렬(Parallel)**: `total_ms: 417.47`, `avg_ms_per_token: 4.17`

---

## 📝 분석 및 결론

- **병렬 처리 효율성**: DB 쿼리 및 FCM 전송 요청 시 병렬 처리(예: `Promise.all`)를 적용하면 전체 지연 시간이 획기적으로 줄어듭니다.
- **확장성**: 수신자 수가 늘어날수록 순차 방식과 병렬 방식의 성능 격차는 더욱 커질 것으로 예상됩니다.
- **권장 사항**: 대량 푸시 작업 시 실행 시간을 최소화하고 Cloud Function의 타임아웃을 방지하기 위해 항상 **병렬(Parallel)** 실행 방식을 우선적으로 사용해야 합니다.

_작성일: 2026-02-14_
